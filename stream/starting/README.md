# VEILCORP Starting Soon

Use `stream/starting/index.html` as the browser source.

The route is the animated big-monitor frame, with `assets/starting2-screen.png` layered under the transparent monitor hole. The frame layer uses the rebuilt 80% transparent cutout from `../ani-bg/assets/80-screen-cutout.webm`.

Use `3440 x 1440` in OBS.
