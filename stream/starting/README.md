# VEILCORP Starting Soon

Use `stream/starting/index.html` as the browser source.

The browser-source overlay uses `assets/ani-bg-80-cutout.webm`, with the baked monitor screen removed from the frames, plus `assets/ani-bg-80-mask.png` as a browser-mask fallback. `assets/starting2-screen.png` is layered underneath the monitor cutout.

`assets/starting-nested.png` is retained as a static fallback/reference.
