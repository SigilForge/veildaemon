# VEILCORP Starting Soon

Use `stream/starting/index.html` as the browser source.

The browser-source overlay uses a silent animated room pass masked by `assets/ani-bg-80-mask.png`, with `assets/starting2-screen.png` layered underneath the monitor cutout.

`assets/starting-nested.png` is retained as a static fallback/reference.
