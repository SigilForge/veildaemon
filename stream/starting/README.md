# VEILCORP Starting Soon

Use `stream/starting/index.html` as the browser source.

The route is the animated big-monitor frame, with `assets/starting2-screen.png` layered under the transparent monitor hole. The original silent frame video loops above it with `assets/ani-bg-80-mask.png` applied as a browser mask.

Use `3440 x 1440` in OBS.
