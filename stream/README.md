# Stream overlays (hosted OBS browser sources)

Live VeilDaemon routes under `/stream/**` for broadcast composition.

## Rules

See root `AGENTS.md` → **Stream Overlay Notes**:

- Hosted browser sources, not local-only scraps  
- Prefer `index.html` routes  
- `noindex`, transparent by default, canvas-sized  
- Bump cache query strings after asset changes  

## Ritual Sites breadcrumb

Venue-scale **Ritual Sites** are a **future vertical**. Design ore and the v0.9 stream-first thesis live in:

**`Docs/Ritual Sites/`**

- Field notes (Evanescence / Morton → layered planes, silhouette-as-source, kinetic light architecture)  
- Implementation map for when programming starts  

Do not invent a full venue control system inside random overlay folders. When Ritual Site v0.9 work begins, extend **scene/cue logic** that can later map to LED walls, scrims, lights, AR, and phones — starting from OBS-like composition using these routes as the authority / apparition / kinetic planes.

## Feed the Daemon breadcrumb

**`Docs/Feed the Daemon/`** — 2026 product spine: public **Sheep Node** (collective web feeder) that can later supply motif / Frequency / Presence state into stream overlays and Ritual Site cues. Same rule: the daemon alters perception; it is not a mascot layer.
